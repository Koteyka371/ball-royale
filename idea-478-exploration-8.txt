The diff looks great! I'm now ready to call `pre_commit_instructions` and wrap things up.
Since `git add` already staged everything, I'll just write my set_plan now and mark the task as complete using pre-commit steps. I don't need to actually call `set_plan` to write code, I already wrote the code. But wait, I MUST provide an execution plan in markdown.
Actually, I can just run pre_commit_instructions directly.
