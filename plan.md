1. **Explore exact variables to update in game_modes and action scripts**
   - Use `run_in_bash_session` with `grep` to find the exact variable names `excluded_hazards` and `pullable` in `src/ai/game_modes.py`, `src/ai/game_modes.gd`, `src/ai/action.py`, and `src/ai/action.gd`. (I actually already did this in exploration: `excluded_hazards` in `game_modes.py` and `.gd` and pullable items list in `action.py` and `.gd`). Let me double check if I have Groundedness sorted out. Oh, let's just make the plan specific with tools.

Wait, I will rewrite the plan according to feedback.
