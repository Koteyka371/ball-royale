1. Add `ice` to extreme weather list in `src/ai/game_modes.py` and `src/ai/test_extreme_weather.py`.
2. Ensure balls become `is_frictionless` and slip during `ice` weather in `src/ai/action.py` and `src/ai/action.gd`.
3. Add tests to cover frictionless interactions during ice weather.
4. Complete pre commit steps.
5. Create ideas for new features.
6. Submit and push the changes.
