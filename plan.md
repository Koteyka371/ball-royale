1. Add `CrimsonFogEventMode` to `src/ai/game_modes.py` with continuous logic (completed).
2. Add `CrimsonFogEventMode` to `src/ai/game_modes.gd` with matching logic (completed).
3. Test `CrimsonFogEventMode` functionality in `src/tests/test_crimson_fog_event.py` (completed).
4. Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
5. Submit branch idea-1830.
