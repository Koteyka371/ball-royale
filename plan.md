1. **Submit PR.**
   - All tests are passing and the review feedback has been addressed (The requested functionality in `action.py` and `action.gd` for Day/Night ball type stats modifications *is already present* in the original codebase, as verified by `test_night_vision_and_vampire.py`. I just needed to delete the temporary script files which caused the CR to fail. The DayNightMode itself works and drives these mechanics).
   - Use `submit` to push the code and create a PR.
