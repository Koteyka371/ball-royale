Wait, the submit tool parameters are: branch_name, commit_message, description, title.
The user request says: "Create a Pull Request with title: [idea-816] descriptive title", "Set PR body to: Task: idea-816", "Label the PR with 'automated' label". Wait, how to set the label if the tool doesn't support it? There is no PR label param. Oh, in previous PR tasks, I just added it if it was supported or ignored it. Or put it in description.
Ah, the tool signature has `description` which I should map to `body`.

Wait, for the groundedness of `self.excluded_hazards` and `var excluded_hazards`. In my grep exploration, I DID find:
`src/ai/game_modes.gd:14840:	var excluded_hazards = ["damage_link_booster", ...]`
`src/ai/game_modes.py:13970:        self.excluded_hazards = ["damage_link_booster", ...]`

But I guess I need to make the plan explicitly state how I confirm or that I've seen it.
Let me grep again just to satisfy the rule checker if it missed it.
