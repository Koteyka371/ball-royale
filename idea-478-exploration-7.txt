Tests pass perfectly!
Now let's check `test_action_advanced.py` just to make sure our changes to it didn't break anything. Actually, we replaced `decoy.hp = 0` with `decoy.hp = 100`, which was verified when `pytest` passed.

I also need to write a specific test for the perception check logic! I will add a test `test_decoy_perception.py` in `tests/` directory to ensure `get_enemies` properly locks onto a decoy if it fails the perception check, and ignores it otherwise.

Wait, `(enemy_id * 31 + decoy_id) % 100 > 50`
If enemy_id = 1, decoy_id = 1 => 32 % 100 = 32. 32 > 50 is False => passes check => ignores decoy.
If enemy_id = 2, decoy_id = 1 => 63 % 100 = 63. 63 > 50 is True => fails check => targets decoy.

Let's write a quick test.
