Added a test suite for `WindFunnelsMode`. The original implementation of `WindFunnelsMode` is already fully integrated and implemented in both `src/ai/game_modes.py` and `src/ai/game_modes.gd`. No bugs found.
