{
  "title": "Tag Team Mode",
  "description": "A new game mode where players queue as a team of two balls but only one is active at a time. The active ball can swap with their teammate using a tag mechanism, which could have a cooldown or require returning to a specific zone. This allows for strategic combinations of different ball types and requires careful management of both balls' health."
}
