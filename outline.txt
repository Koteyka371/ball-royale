The task is to replace the smooth circular shrinking zone with an arena made of hexagonal tiles that independently glow red and fall away, reducing the safe map area into fragmented islands that players must jump between.

So instead of `self.safe_zone_radius`, the arena is composed of hexagonal tiles.
Wait, "jump between" - is there a jumping mechanic? Or does it just mean navigate between? The players are balls that roll around, so they might "jump" or dash, or just move.

The shrinking zone logic is currently in `basic_arena.py` (and probably `basic_arena.gd`, `procedural_arena.py/gd`). Let's read `procedural_arena.py` too.
