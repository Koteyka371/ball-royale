The task is to introduce a deployable 'Decoy' that mimics the player's current ball type and health. AI enemies will occasionally lock onto the decoy if they fail a perception check, absorbing damage while giving the player an opportunity to escape or ambush.

Currently, there are two related concepts:
- "decoy_item" hazard
- "deploy_decoy" and "deploy_decoy_beacon" skills.

The task is to:
"Introduce a deployable 'Decoy' that mimics the player's current ball type and health."

We already have `deploy_decoy` skill which:
- Swaps position with an active decoy
- Or spawns 2 decoys and orbits them

Wait, the task says:
"mimics the player's current ball type and health. AI enemies will occasionally lock onto the decoy if they fail a perception check..."

Right now, enemies ignore targets with `is_decoy = True`!
Let's look at `action.py` where we filter targets.
```python
valid_targets = [b for b in balls if getattr(b, "alive", True) and b != self.ball and not getattr(b, "is_decoy", False)]
```
If enemies occasionally lock onto the decoy *if they fail a perception check*, we need to change how enemies select targets! They shouldn't outright filter out `is_decoy=True`.

Let's read the task again:
"Introduce a deployable 'Decoy' that mimics the player's current ball type and health. AI enemies will occasionally lock onto the decoy if they fail a perception check, absorbing damage while giving the player an opportunity to escape or ambush."

Actually, maybe I should create a new ball type, or maybe just add a new skill called `deployable_decoy`?
Or maybe update the existing decoy logic?

Let's see if we should create a new ball type. I could create `ball_types_illusionist.py`.
Wait, "Introduce a deployable 'Decoy'". It might be a new skill.
Let's see where skills are defined.
Skills are in `action.py` and `action.gd`.

Let's check the current `deploy_decoy` skill.
