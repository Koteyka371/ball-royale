--- tests/simulate_battle.py
+++ tests/simulate_battle.py
@@ -370,6 +370,12 @@
                 for line in lines[-10:]:
                     print(f"  > {line}")
                 print(f"{'='*60}")
+
+                print("\n" + "="*60)
+                print("  UI KILL FEED PREVIEW")
+                print("="*60)
+                for msg in commentator.kill_feed.get_messages():
+                    print(f"  [UI] {msg}")
             except ImportError:
                 pass
