We see that it returns 1 enemy, which is the decoy. This is because `is_decoy` entities are currently unconditionally added to the enemy list if they are of a different team/type!

Let's look at `_get_enemies_internal` again:
```python
        if hasattr(self.world, "balls"):
            for b in self.world.balls:
                if getattr(b, "is_decoy", False) and getattr(b, "alive", True) and b not in enemies:
                    if getattr(b, "ball_type", None) != self.ball.ball_type:
                        dx = getattr(b, "x", 0) - self.ball.x
                        dy = getattr(b, "y", 0) - self.ball.y
                        if dx*dx + dy*dy <= perception_radius*perception_radius:
                            enemies.append(b)
```

The task states: "AI enemies will occasionally lock onto the decoy if they fail a perception check".
So if they SUCCEED the perception check, they should NOT append `b` to `enemies`!
Also, `deploy_decoy` in `action.py` and `action.gd` shouldn't just set `hp = max_hp * 0.1` but `hp = self.ball.hp` and `max_hp = self.ball.max_hp`.

Let's look at where `deploy_decoy` or `decoy_item` are used.
`action.py` -> `decoy_item` collected:
```python
                elif getattr(nearest, "kind", None) == "decoy_item":
                    import copy
                    if hasattr(self.world, "balls"):
                        decoy = copy.copy(self.ball)
                        decoy.id = getattr(self.world, "next_id", random.randint(10000, 99999))
                        decoy.hp = getattr(self.ball, "hp", 100) # changed from max_hp * 0.1
                        decoy.max_hp = getattr(self.ball, "max_hp", 100)
                        decoy.damage = 0
                        decoy.is_decoy = True
                        decoy.decoy_timer = 5.0
                        self.world.balls.append(decoy)
```
Wait, the task says: "Introduce a deployable 'Decoy' that mimics the player's current ball type and health."

There are a few places where a decoy is deployed:
1. `action.py`: `decoy_item` collection.
2. `action.py`: `deploy_decoy` skill.
3. `action.py`: `deploy_decoy_beacon` skill.
4. `action.gd`: `decoy_item` collection.
5. `action.gd`: `deploy_decoy` skill.
6. `action.gd`: `deploy_decoy_beacon` skill.
7. `game_modes.gd`: sometimes deploys a decoy?

Wait, in `game_modes.gd` at line 2146:
```gdscript
								if b.has_method("duplicate"): decoy = b.duplicate()
								elif b is Dictionary: decoy = b.duplicate()
								if decoy != null:
									if "id" in decoy: decoy.id = randi() % 90000 + 10000
									if "max_hp" in decoy: decoy.max_hp = mhp * 0.1
									if "hp" in decoy: decoy.hp = mhp * 0.1
```
This is probably for the `coach_mode.gd` or `test_nemesis.py` or some specific mode event.

We should fix all occurrences of decoy creation to mimic current health instead of 10% max health.
Wait, if `deploy_decoy` is a skill, who uses it? `ball_types_trickster.py` uses `shoot_portals`. What ball uses `deploy_decoy`?
Let's grep for `deploy_decoy`.
