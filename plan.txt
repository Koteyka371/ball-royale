1. **Add `link` trap variant in `src/system/lobby.py` and `src/system/lobby.gd`**
   - Use `read_file` to verify file changes.
   - Add `"link"` to the allowed variants in `select_trap_variant` in both files. (e.g. `src/system/lobby.py` line 24, `src/system/lobby.gd` line 30).

2. **Implement `link` trap functionality in `src/ai/action.py`**
   - Add the logic to the hazard parsing in `action.py`.
   - Before `elif trap_variant == "tar":` in `src/ai/action.py`, add logic for `elif trap_variant == "link":`.
   - The logic finds a random alive enemy entity.
   - Set `self.ball.trap_link_target = random_enemy` and `self.ball.trap_link_timer = 10.0`.
   - Set `hazard.duration = 0.0`.
   - Near where `self.ball.prev_hp` is captured and compared, implement the trap_link mirroring damage logic:
     - `if getattr(self.ball, "trap_link_timer", 0.0) > 0.0:`
     - `self.ball.trap_link_timer -= delta`
     - `trap_link_target = getattr(self.ball, "trap_link_target", None)`
     - Compare `hp_diff = self.ball.prev_hp - self.ball.hp`
     - If `hp_diff > 0` and `trap_link_target` is alive and not `_is_trap_link_syncing`:
     - Apply damage to `trap_link_target`: `trap_link_target.hp -= hp_diff`
     - (Ensure we mark `trap_link_target._is_trap_link_syncing = True` before damage to prevent infinite loops, if needed)
   - Ensure the HP logic happens correctly around line 3200 in `action.py`. Let's actually add the timer logic around the `entangled_with_id` logic.

3. **Implement `link` trap functionality in `src/ai/action.gd`**
   - Similar to `action.py`, find the hazard loop where traps are handled, and add `elif trap_variant == "link":`.
   - Find random alive enemy, set `my_ball.set_meta("trap_link_target", enemy)` and `my_ball.set_meta("trap_link_timer", 10.0)`.
   - In the section where `prev_hp` is compared (around line 5814 in `action.gd`), add the trap link mirroring logic.
   - Decrease `trap_link_timer` by delta. Check `hp_diff = prev_hp - my_ball.hp`. If `hp_diff > 0`, deal it to the target.

4. **Write tests**
   - Create a test file `src/ai/test_link_trap.py` that verifies the `link` trap functionality.
   - Ensure tests pass with `pytest`.

5. **Generate ideas in `ideas/` directory**
   - Create at least 2 valid JSON files in the `ideas/` directory with new game features (e.g., `ideas/idea_idea-982_1.json`, `ideas/idea_idea-982_2.json`).

6. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.**
   - Run `pre_commit_instructions` tool and follow the returned instructions.

7. **Create Branch**
   - Run `git checkout -b idea-982`.
8. **Commit Changes**
   - Run `git add .` and `git commit -m "Add link trap variant"`.
9. **Push Branch**
   - Run `git push origin idea-982`.
10. **Create Pull Request**
   - Run `submit` tool with title `[idea-982] Add link trap`, body `Task: idea-982`, branch `idea-982` and label `automated`.
