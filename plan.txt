1. Create `src/ai/ball_types_leech.py` with Leech class: base dmg low, heals for 200% of damage dealt, skill 'leech_tether'.
2. Create `src/ai/ball_types_leech.gd` matching the Python version.
3. Update `_deal_damage` (actually in `_attack` or general damage processing logic) in `action.py` to allow Leech to heal 200% of dealt damage. Wait, `action.py` has a `target.hp -= damage` spot. If `attacker.ball_type == 'leech'` and `attacker.hp < attacker.max_hp`: `attacker.hp = min(attacker.max_hp, attacker.hp + damage * 2.0)`. Update both `action.py` and `action.gd`.
4. Update `action.py` and `action.gd` to handle the `leech_tether` skill. When used, the Leech tethers to the nearest enemy.
   - Skill logic (`elif skill_name == "leech_tether":`): find nearest enemy. Set `attacker.leech_tether_target = target`, `attacker.leech_tether_timer = 3.0` (or some duration).
   - Tick logic: if `leech_tether_timer > 0`, reduce timer, if target is alive and in range (e.g. `dist < 200`), deal drain damage per tick (e.g., `drain_damage = 5.0 * delta * 60`), heal attacker by `drain_damage`, slow the target (e.g. `target.speed_multiplier = 0.5` or modify speed directly like in tether hook). Wait, tether hook uses a specific implementation. I will look at how `magnet_tether` is implemented.
5. Create tests in `test_leech_ball.py`.
6. Add new ideas to JSON files.
