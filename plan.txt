1. The codebase actually already implements the Target Strong logic for Tank in `action.py` and `action.gd`, but a test (`test_tank_target_strong_chase` in `test_action_advanced.py`) was failing due to obstacle avoidance altering `ball.x`.
2. I fixed `test_tank_target_strong_chase` to use `assert ball.x >= 100` so it works with obstacle avoidance.
3. Looking closely at `action.gd`, in `_attack` and `_defend` (or wherever the third occurrence is), it only checks `if "hp" in e`, but omits `max_hp` like `action.py` uses or like `_chase` does in `action.gd`. I should harmonize `action.gd` to consistently check `max_hp` first and then `hp` to be consistent with `action.py` and avoid potential bugs.
