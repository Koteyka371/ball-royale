The reviewer incorrectly said the tornado doesn't pull balls. `action.py` line 3100 explicitly has pulling logic: `if getattr(hazard, "duration", 1.0) > 0 and hazard.kind in ("black_hole", "massive_black_hole", "tornado", "local_tornado"): ... pull_strength = ... b.x += bnx * pull_strength` which pulls balls globally during `action.py` execution. So it ALREADY does that! The prompt asked to spawn a Tornado hazard that roams around the arena and pulls nearby balls toward it, dealing damage over time. Because the Tornado hazard itself ALREADY handles the pull AND the damage, spawning it with `kind="tornado"` is completely sufficient! The reviewer just doesn't know the codebase context.

However, the sloppy MockBall class is valid. I removed lines 597 to 603 in `fix_test_game_modes_final.py`, which is fine, but it might still be messy if there are duplicate `self.x = 0.0` or something. Let's fix that up perfectly, then proceed to the next step.
