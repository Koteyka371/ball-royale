1. We need to implement `TimeDilationZoneMode` in `src/ai/game_modes.py` and `src/ai/game_modes.gd`.
2. The mode should spawn a zone in the arena. In this zone, entities, projectiles, and status effects move and expire at half their normal rate.
3. To implement this, we should add a `TimeDilationZoneMode` inheriting from `GameMode`.
4. In `setup`, create a hazard named "time_dilation_zone" with `damage = 0.0` in the center of the arena (or random). Wait, the prompt says "An anomalous zone in the arena". We'll create one hazard of kind "time_dilation_zone".
5. In `tick`, we find this zone. For every ball inside, we cut its speed in half.
6. To slow down projectiles and status effects at half rate, we need to inspect what `Action` does, or we can just apply a buff to balls inside the zone, or handle it in `TimeDilationZoneMode.tick`. Wait, if we just reduce `speed` to half, that covers entities. What about projectiles?
7. Projectiles are often treated as balls with `ball_type == "projectile"` or in `world.projectiles` or spawned by hazards. Wait, `world.projectiles`? There is no `world.projectiles`, usually it's `balls` with `ball_type == "projectile"`. Let's check how projectiles are tracked in the codebase.
8. Status effects: `b.frozen_timer`, `b.burn_timer`, `b.speed_boost_timer`, etc. We can find all timers in `b` and increment them by `delta / 2` (because they normally decrement by `delta` every tick in Action or other places, so we effectively slow them down by refunding half the delta).
